# homebrew-unpdf

Homebrew tap for the `unpdf` PDF-to-HTML converter.

```console
brew install erikbra/unpdf/unpdf
```

The formula supports Apple Silicon and Intel macOS, plus x64 and ARM64
Linuxbrew. Preview binaries are explicitly unsigned; Homebrew verifies the
SHA-256 checksum published with each GitHub release.
